spring:
  application:
    name: Messages Shop
  datasource:
    url: ${DEV_SQL_DB_URL}
    username: ${DEV_SQL_DB_USERNAME}
    password: ${DEV_SQL_DB_PASSWORD}
    driver-class-name: ${DEV_SQL_DB_DRIVER}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: false

server:
  port: 8080
